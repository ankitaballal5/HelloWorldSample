package com.example;

public class GreetingService {
    public String greet(String name) {
        return "Hello, " + name + "!";
    }
}

/*
 * Summary of changes:
 * No changes were made to the code as it does not contain any functionality related to the Guava library.
 * The context provided involved upgrading Guava dependency, but since this code does not use Guava, it remains unchanged.
 */